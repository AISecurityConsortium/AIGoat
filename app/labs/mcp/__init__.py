"""MCP (Model Context Protocol) labs.

Shipped stdio servers live in ``app/mcp_servers/``. The in-app client is
``mcp.client`` (``app/mcp/`` + ``app/surfaces/mcp_client.py``). This package
does not simulate the protocol.
"""
